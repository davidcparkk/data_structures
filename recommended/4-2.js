// Trees and Graphs
// Minimal Tree
// Given a sorted (increasing order) array with unique integer elements, write an algo to create a binary search tree with min height

let insertBalanced = function(array) {
  
}